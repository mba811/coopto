<?xml version="1.0" encoding="ISO-8859-1" standalone="yes"?>
<dunes-script-module name="getAccessModes" result-type="Array/string" api-version="3.1.0"  id="e8a45d33-179e-4d34-8e6f-576f41654d40"  version="0.0.1"  allowed-operations="vef" ><description><![CDATA[Return configuration element keys for predefined workflow elements.]]></description>
<script encoded='false'><![CDATA[var returnArray = [];
var configpath = Server.getConfigurationElementCategoryWithPath("Library/Coopto/Container/enums");
if(configpath != undefined && configpath != null)
{
	var elements = configpath.configurationElements;
	for each (element in elements)
	{
		if(element.name == "AccessModes")
		{
			var attributes = element.attributes;
			for each(attribute in attributes)
			{
				returnArray.push(attribute.name);
			}
			return returnArray;
		}
	}
}
else
{
	throw("Error: the configuration element was not found.");
}]]></script>
</dunes-script-module>